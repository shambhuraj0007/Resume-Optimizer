# Media Logos

Drop press / media logos here (SVG preferred, or compressed PNG).

These are referenced in `config/socialProof.ts` → `mediaLogoStripConfig.logos[].logoUrl`.

Example reference: `/media/economic-times.svg`
